set(CMAKE_POLICY_DEFAULT_CMP0077 NEW)

include(FetchContent)

FetchContent_Declare(
  allegro
  GIT_REPOSITORY https://github.com/liballeg/allegro5.git
  GIT_TAG        5.2.9.1
)

set(WANT_EXAMPLES OFF)
set(WANT_DEMO OFF)
set(WANT_TESTS OFF)
set(WANT_DOCS OFF)

set(WANT_AUDIO OFF)
set(WANT_TTF OFF)
set(WANT_COLOR OFF)
set(WANT_MEMFILE OFF)
set(WANT_PHYSFS OFF)
set(WANT_NATIVE_DIALOG OFF)
set(WANT_VIDEO OFF)

FetchContent_MakeAvailable(allegro)
